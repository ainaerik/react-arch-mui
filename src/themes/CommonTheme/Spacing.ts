import { ThemeOptions } from '@mui/material/styles'

/**
 * Spacing
 * e.g :
 * theme.spacing(2); => `${8 * 2}px` = '16px'
 * theme.spacing(1, 2); => '8px 16px'
 */
const SpacingOptions: ThemeOptions = {
  spacing: 8,
}

export default SpacingOptions
